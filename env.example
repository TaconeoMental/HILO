# Flask
FLASK_ENV=development
FLASK_SECRET_KEY=change-this-to-a-random-secret-key-in-production
FLASK_APP=app.py

# Database
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=hilo
POSTGRES_PASSWORD=change-this-password-in-production
POSTGRES_DB=hilo

# Redis / RQ
REDIS_URL=redis://redis:6379/0
RQ_AUDIO_QUEUE=hilo_audio
RQ_TRANSCRIBE_QUEUE=hilo_transcribe
RQ_PHOTO_QUEUE=hilo_photos
RQ_LLM_QUEUE=hilo_llm

# Sesión
SESSION_LIFETIME_DAYS=1

# Cookies
COOKIE_SECURE=0
COOKIE_SAMESITE=Lax

DATA_DIR=data
RETENTION_DAYS=90

# Audio ingest / STT
AUDIO_WS_PATH=/ws/audio
AUDIO_STORAGE_BACKEND=disk
S3_AUDIO_BUCKET=
S3_AUDIO_PREFIX=audio
AUDIO_CHUNK_SECONDS=10
TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
TRANSCRIBE_PARALLEL_WORKERS=4
TRANSCRIBE_CHUNK_TIMEOUT=30
TRANSCRIBE_JOB_TIMEOUT=300
AUDIO_PREP_JOB_TIMEOUT=300

# OpenAI
OPENAI_API_KEY=sk-your-api-key-here
LLM_MODEL=gpt-4.1-mini
IMAGE_STYLE_ENABLED=false

STYLIZE_PARALLEL_WORKERS=8
STYLIZE_PHOTO_TIMEOUT=60
PHOTO_JOB_TIMEOUT=300
LLM_JOB_TIMEOUT=600
MAX_IMAGE_SIZE=2097152
MAX_CHUNK_SIZE=5242880
DEBUG_CONCAT_FILES=0
